apiVersion: v1
kind: Service
metadata:
  name: {{ include "rallly-cleaned.fullname" . }}
  labels:
    {{- include "rallly-cleaned.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      protocol: TCP
  selector:
    {{- include "rallly-cleaned.selectorLabels" . | nindent 4 }}
