apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rallly-cleaned.fullname" . }}-test-connection"
  labels:
    {{- include "rallly-cleaned.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rallly-cleaned.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
